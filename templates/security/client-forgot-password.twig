{% extends 'base.html.twig' %}

{% block title %}Aty'place oublie de mot de passe{% endblock %}

{% block body %}
    {% for message in app.flashes('error') %}
        <div class="alert aler-notice">{{ message }}</div>
    {% endfor %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}